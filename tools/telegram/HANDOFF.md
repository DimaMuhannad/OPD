# Передача работы: публикация в Telegram (состояние на 25.09.2026)

## Что сделано
- Посты к занятию 3 (19.09) готовы: `tools/telegram/posts/`, два комплекта
  (`2026-09-19_iniciaciya-listy/`, `2026-09-19_pasport/`), разложены по темам
  группы «Объявления / Материалы / Домашнее задание / Литература».
  Какие файлы из `zanyatiya/` прикладывать — написано в постах «Материалы».
- Порядок публикации — `tools/telegram/posts/README.md`: сначала
  «Материалы», «ДЗ», «Литература»; «Объявления» последними, со ссылками
  `https://t.me/c/<id без -100>/<тема>/<сообщение>` на уже отправленные.
- api.telegram.org из облачного контейнера доступен (проверено).

## Что сделал преподаватель
- Создал бота через @BotFather (один бот на обе группы).
- Добавил его администратором в обе группы: м631к (1 курс) и м531 (2 курс).
- Токен положил в переменную окружения `TG_BOT_TOKEN` в настройках облачного
  окружения. **Токен никогда не просить в чате и не печатать в вывод.**
- Написал по тестовому сообщению в каждую тему обеих групп.

## Что сделать в новой сессии
1. Проверить, что `TG_BOT_TOKEN` задан (`test -n "$TG_BOT_TOKEN"`, значение не
   выводить); `getMe` — имя бота.
2. `getUpdates` → ID обеих групп (`chat.id`, вида -100…) и `message_thread_id`
   каждой темы (название темы — в `reply_to_message.forum_topic_created.name`
   или в сервисном сообщении). Записать таблицу группа × тема → ID в этот файл
   (ID не секрет, токен — секрет).
3. Доработать `telegram_publish.py`: параметр `thread_id` →
   `message_thread_id` во всех send_* и send_poll; публикация комплекта
   постов из папки по темам; для «Объявлений» — подстановка ссылок на
   отправленные сообщения по `message_id` из ответов API.
4. Одно тестовое сообщение в тему, которую назовёт преподаватель; затем
   удалить его (`deleteMessage`).
5. Публиковать посты в группы **только после явного подтверждения**
   преподавателя — это внешнее действие, его не отменить для тех, кто
   уже прочитал.

## Правила
- Ветка `claude/archive-to-repository-heyt7z`, PR не создавать.
- Тон и словарь постов — по `CLAUDE.md` (раздел «Тон»): «сложности»,
  «ещё один подход», не «не получилось / переделать».
- Литература — только русскоязычная, в свободном доступе, ссылки проверены.
- Паспорт на паре 19.09 НЕ заполняли — это ДЗ к 26.09 (на пару или в чат).

## Дальше по плану
- Занятие 4 (26.09): стейкхолдеры, представление проектов, go/no-go —
  строка недели 4 в `PLAN.md`.
- Тест 1 (10.10): дописать блок про стейкхолдеров в `testy/`.
- Сверить шкалу 85/70/55 с действующим ЛНА ГУАП.
